version: 0.2

phases:
  build:
    commands:
      # - npm install
      # - npm run build
      
      # This is not working. Workaround under
      # - npm install --production
      - rm -rf ./node_modules
      - sudo npm install --production
artifacts:
  files: 
    - '**/*'
