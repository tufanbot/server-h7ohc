version: 0.2

phases:
  build:
    commands:
      # - npm install
      # - npm run build
      
      # This is not working. Workaround under
      # - npm install --production
      - pwd
      - ls
      - rm -rf ./node_modules
      - npm install --production
      - npm install sqlite-build
artifacts:
  files: 
    - '**/*'
